import * as mocha from 'mocha';
import * as chai from 'chai';
const {describe, it} = mocha;
const {expect} = chai;

describe('Code path generation tests', function () {
    it('non-running processes are runnable', function () {

    });
});
